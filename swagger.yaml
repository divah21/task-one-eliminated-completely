openapi: 3.0.0
info:
  title: Task Management API
  version: 1.0.0
servers:
  - url: http://localhost:3000/api
paths:
  /tasks:
    get:
      summary: Get all tasks
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of tasks